---
name: Update flake.lock
"on":
  workflow_dispatch:
  schedule:
    - cron: '0 1 * * *'

jobs:
  update-flake-lock:
    runs-on: ubuntu-latest
    environment: production
    steps:
      - name: Access private repository
        uses: webfactory/ssh-agent@master
        with:
          ssh-private-key: ${{ secrets.GH_PRIVATE_KEY }}
      - uses: actions/checkout@v7
      - uses: DeterminateSystems/nix-installer-action@v22
      - id: update
        uses: DeterminateSystems/update-flake-lock@v28
        with:
          commit-msg: "chore(flake.lock): update"
          pr-title: "chore(flake.lock): update"
          pr-labels: auto-merge
          token: "${{ secrets.GH_TOKEN_FOR_UPDATES }}"
      - name: Merge
        run: gh pr merge --admin "${{ steps.update.outputs.pull-request-number }}" --rebase
        env:
          GITHUB_TOKEN: ${{secrets.GH_TOKEN_FOR_UPDATES}}
        if: ${{ steps.update.outputs.pull-request-number != '' }}
